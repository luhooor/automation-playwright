/**
 * Barrel export for configuration modules.
 *
 * Usage:
 *   import { getEnvironment, environments } from "#configs";
 */

export { getEnvironment, environments } from "#configs/env";
export type { EnvConfig, EnvironmentName } from "#configs/env";
